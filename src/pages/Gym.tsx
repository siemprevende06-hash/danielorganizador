import { useState, useEffect, useCallback, useRef } from "react";import { Card, CardContent } from "@/components/ui/card";import { Button } from "@/components/ui/button";import { Badge } from "@/components/ui/badge";import { Input } from "@/components/ui/input";import { cn } from "@/lib/utils";import { supabase } from "@/integrations/supabase/client";import { useWorkoutTracking, TrainingType, computeSessionSummary } from "@/hooks/useWorkoutTracking";import { Check, ChevronRight, Minus, Plus, Timer, Dumbbell, Play, SkipForward, Trophy, ChevronDown, ChevronUp, Box } from "lucide-react";import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";import { toast } from "@/hooks/use-toast";import { StrengthGoalsCard } from "@/components/gym/StrengthGoalsCard";import { GymStatsView } from "@/components/gym/GymStatsView";import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs";const formatElapsed = (total: number) => { const m = Math.floor(total / 60); const s = total % 60; return `${String(m).padStart(2, "0")}:${String(s).padStart(2, "0")}`; };interface ActiveSet {  exerciseIndex: number;  setIndex: number;}interface SetData {  weight: number;  reps: number;  completed: boolean;}interface WorkoutSession {  exerciseIndex: number;  sets: SetData[][];}const MUSCLE_GROUPS = [  "Pecho", "Espalda", "Hombros", "B├¡ceps", "Tr├¡ceps",  "Piernas", "Abdominales", "Gl├║teos", "Cardio"];const DAY_KEYS = ["monday", "tuesday", "wednesday", "thursday", "friday", "saturday", "sunday"];const DAY_LABELS: Record<string, string> = {  monday: "Lunes", tuesday: "Martes", wednesday: "Mi├®rcoles",  thursday: "Jueves", friday: "Viernes", saturday: "S├íbado", sunday: "Domingo"};export default function Gym() {  const {    routine, allRoutines, exercises, logs, isLoading,    createRoutine, addExercise, removeExercise, logWorkout, selectRoutine,    getTodayWorkout, getExercisesByDay, reload, DAY_NAMES, loadAllRoutines, sessions, startSession, finishSession, syncWorkoutDuration  } = useWorkoutTracking();  const [trainingType, setTrainingType] = useState<TrainingType>('gimnasio');  const [editingLog, setEditingLog] = useState<string | null>(null);  const [editWeight, setEditWeight] = useState("");  const [editReps, setEditReps] = useState("");  const updateLogEntry = async (logId: string, weight: number | null, reps: number[]) => {    const { error } = await supabase      .from('exercise_logs')      .update({ weight_kg: weight, reps_per_set: reps })      .eq('id', logId);    if (!error) {      toast({ title: "Progreso actualizado" });      reload();    }    setEditingLog(null);  };  const deleteLogEntry = async (logId: string) => {    const { error } = await supabase      .from('exercise_logs')      .delete()      .eq('id', logId);    if (!error) {      toast({ title: "Entrada eliminada" });      reload();    }  };  const [activeExercise, setActiveExercise] = useState(0);  const [sessionSets, setSessionSets] = useState<SetData[][]>([]);  const [restTimer, setRestTimer] = useState<number | null>(null);  const [restDuration, setRestDuration] = useState(60);  const [workoutStarted, setWorkoutStarted] = useState(false);  const sessionStartRef = useRef<number | null>(null);  const [activeSessionId, setActiveSessionId] = useState<string | null>(null);  const [elapsedSeconds, setElapsedSeconds] = useState(0);  const [expandedSession, setExpandedSession] = useState<string | null>(null);  const [workoutComplete, setWorkoutComplete] = useState(false);  const timerRef = useRef<ReturnType<typeof setInterval> | null>(null);  const [setupOpen, setSetupOpen] = useState(false);  const [newRoutineName, setNewRoutineName] = useState("");  const [selectedDays, setSelectedDays] = useState<Record<string, boolean>>({});    const [addExOpen, setAddExOpen] = useState(false);  const [newExName, setNewExName] = useState("");  const [newExDay, setNewExDay] = useState("");  const [newExSets, setNewExSets] = useState(5);  const [newExReps, setNewExReps] = useState("8");  const [newExMuscle, setNewExMuscle] = useState("");  const [newExWeight, setNewExWeight] = useState(0);  const [viewDay, setViewDay] = useState<string | null>(null);  const [expandedEx, setExpandedEx] = useState<string | null>(null);  const todayWorkout = routine ? getTodayWorkout() : null;  const sessionSummaries = sessions.map(s => computeSessionSummary(s, exercises));const sessionTotals = sessionSummaries.reduce((acc, s) => ({ minutes: acc.minutes + (s.duration_minutes || 0), volume: acc.volume + s.totalVolumeKg, sets: acc.sets + s.setCount }), { minutes: 0, volume: 0, sets: 0 });  useEffect(() => {    reload(trainingType);  }, [trainingType]);  useEffect(() => {    if (todayExercises.length > 0 && sessionSets.length === 0) {      setSessionSets(        todayExercises.map(ex =>          Array.from({ length: ex.target_sets }, () => ({            weight: trainingType === 'gimnasio' ? 0 : 0,            reps: parseInt(ex.target_reps) || 8,            completed: false,          }))        )      );    }  }, [todayExercises.length, trainingType]);  useEffect(() => {    if (restTimer !== null && restTimer > 0) {      timerRef.current = setInterval(() => {        setRestTimer(prev => (prev !== null && prev > 0 ? prev - 1 : 0));      }, 1000);    } else if (restTimer === 0) {      if (timerRef.current) clearInterval(timerRef.current);    }    return () => { if (timerRef.current) clearInterval(timerRef.current); };  }, [restTimer !== null && restTimer > 0]);  useEffect(() => { if (!workoutStarted || workoutComplete) return; const t = setInterval(() => { if (sessionStartRef.current) setElapsedSeconds(Math.floor((Date.now() - sessionStartRef.current) / 1000)); }, 1000); return () => clearInterval(t); }, [workoutStarted, workoutComplete]);  const completeSet = (exIdx: number, setIdx: number) => {    setSessionSets(prev => {      const next = prev.map(s => [...s]);      next[exIdx] = next[exIdx].map((s, i) => i === setIdx ? { ...s, completed: true } : s);      return next;    });    setRestTimer(restDuration);  };  const skipRest = () => {    setRestTimer(null);    if (timerRef.current) clearInterval(timerRef.current);  };  const adjustRest = (delta: number) => {    setRestDuration(prev => Math.max(5, prev + delta));    setRestTimer(prev => prev !== null ? Math.max(0, prev + delta) : null);  };  const updateSetData = (exIdx: number, setIdx: number, field: "weight" | "reps", value: number) => {    setSessionSets(prev => {      const next = prev.map(s => [...s]);      next[exIdx] = next[exIdx].map((s, i) => i === setIdx ? { ...s, [field]: value } : s);      return next;    });  };  const currentExSets = sessionSets[activeExercise] || [];  const completedSets = currentExSets.filter(s => s.completed).length;  const totalSets = currentExSets.length;  const currentSetIdx = currentExSets.findIndex(s => !s.completed);  const currentExerciseObj = todayExercises[activeExercise];  const allSetsComplete = completedSets === totalSets && totalSets > 0;  const handleNext = () => {    if (restTimer !== null) {      skipRest();      return;    }    if (allSetsComplete) {      if (currentExerciseObj) {        const avgWeight = trainingType === 'gimnasio'          ? currentExSets.reduce((s, v) => s + v.weight, 0) / currentExSets.length          : 0;        logWorkout(          currentExerciseObj.id,          avgWeight,          completedSets,          currentExSets.map(s => s.reps),          undefined,          activeSessionId || undefined,          trainingType === 'gimnasio' ? currentExSets.map(s => s.weight) : undefined        );      }      if (activeExercise < todayExercises.length - 1) {        setActiveExercise(prev => prev + 1);        setRestTimer(null);      } else {        setWorkoutComplete(true);        const totalSecs = sessionStartRef.current ? Math.floor((Date.now() - sessionStartRef.current) / 1000) : elapsedSeconds;        const totalMins = Math.max(1, Math.round(totalSecs / 60));        if (activeSessionId) { finishSession(activeSessionId, totalMins); syncWorkoutDuration(totalMins); }        toast({ title: "­ƒÄë ┬íEntrenamiento completado!", description: "Excelente trabajo hoy." });      }    }  };  const handleCreateRoutine = async () => {    if (!newRoutineName.trim()) return;    await createRoutine(newRoutineName, selectedDays, trainingType);    setSetupOpen(false);    setNewRoutineName("");    setSelectedDays({});    toast({ title: "Rutina creada" });  };  const handleAddExercise = async () => {    if (!routine || !newExName.trim() || !newExDay) return;    await addExercise(routine.id, newExName, newExDay, newExSets, newExReps, newExMuscle || undefined);    setAddExOpen(false);    setNewExName("");    setNewExSets(5);    setNewExReps("8");    setNewExMuscle("");    toast({ title: "Ejercicio agregado" });  };  const handleSelectRoutine = async (routineId: string) => {    await selectRoutine(routineId);    toast({ title: "Rutina seleccionada" });  };  if (isLoading) {    return (      <div className="min-h-screen flex items-center justify-center pt-24">        <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-primary" />      </div>    );  }  return (    <div className="min-h-screen bg-gradient-to-b from-background to-muted/20 p-4 md:p-8 pt-24">      <div className="max-w-2xl mx-auto space-y-4">        {/* Training Type Selector */}        <div className="flex items-center justify-between">          <div className="flex gap-2 bg-muted p-1 rounded-lg">            <button              onClick={() => { setTrainingType('gimnasio'); setWorkoutComplete(false); setWorkoutStarted(false); setActiveExercise(0); setSessionSets([]); }}              className={cn(                "flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-all",                trainingType === 'gimnasio' ? "bg-primary text-primary-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"              )}            >              <Dumbbell className="h-4 w-4" />              Gimnasio            </button>            <button              onClick={() => { setTrainingType('calistenia'); setWorkoutComplete(false); setWorkoutStarted(false); setActiveExercise(0); setSessionSets([]); }}              className={cn(                "flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-all",                trainingType === 'calistenia' ? "bg-primary text-primary-foreground shadow-sm" : "text-muted-foreground hover:text-foreground"              )}            >              <Box className="h-4 w-4" />              Calistenia            </button>          </div>        </div>        {!routine ? (          /* No active routine: show all routines list + create */          <div className="space-y-6">            <div className="text-center space-y-3">              {trainingType === 'gimnasio' ? (                <Dumbbell className="h-12 w-12 mx-auto text-primary" />              ) : (                <Box className="h-12 w-12 mx-auto text-teal-500" />              )}              <h1 className="text-2xl font-bold">                {trainingType === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}              </h1>              <p className="text-muted-foreground">                {allRoutines.length === 0                  ? "No tienes rutinas creadas. ┬íCrea tu primera rutina!"                  : "Selecciona una rutina para empezar a entrenar"}              </p>            </div>            {/* All routines list */}            {allRoutines.length > 0 && (              <div className="space-y-3">                <h2 className="text-sm font-semibold text-muted-foreground uppercase tracking-wider">                  Tus Rutinas ({allRoutines.length})                </h2>                {allRoutines.map(r => {                  const daysActive = Object.entries(r.workout_days || {})                    .filter(([, v]) => v)                    .map(([k]) => DAY_LABELS[k]?.slice(0, 3))                    .join(", ");                  return (                    <Card key={r.id} className="p-4 hover:shadow-md transition-shadow">                      <div className="flex items-center justify-between">                        <div className="space-y-1">                          <div className="flex items-center gap-2">                            <span className="font-semibold">{r.name}</span>                            <Badge variant={r.tipo === 'gimnasio' ? 'default' : 'secondary'} className="text-[10px]">                              {r.tipo === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}                            </Badge>                            {r.is_active && (                              <Badge variant="outline" className="text-[10px] text-green-600 border-green-400">Activa</Badge>                            )}                          </div>                          <p className="text-xs text-muted-foreground">{daysActive || "Sin d├¡as"}</p>                        </div>                        <Button size="sm" onClick={() => handleSelectRoutine(r.id)} disabled={r.is_active}>                          {r.is_active ? "Seleccionada" : "Seleccionar"}                        </Button>                      </div>                    </Card>                  );                })}              </div>            )}            {/* Create button */}            <div className="text-center">              <Dialog open={setupOpen} onOpenChange={setSetupOpen}>                <DialogTrigger asChild><Button size="lg" className="gap-2">                    <Plus className="h-5 w-5" /> Crear Rutina de {trainingType === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}                  </Button></DialogTrigger>                <DialogContent>                  <DialogHeader><DialogTitle>Nueva Rutina de {trainingType === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}</DialogTitle></DialogHeader>                  <div className="space-y-4">                    <Input placeholder="Nombre de la rutina" value={newRoutineName} onChange={e => setNewRoutineName(e.target.value)} />                    <div>                      <p className="text-sm font-medium mb-2">D├¡as de entrenamiento</p>                      <div className="flex flex-wrap gap-2">                        {DAY_KEYS.map(d => (                          <Button                            key={d}                            size="sm"                            variant={selectedDays[d] ? "default" : "outline"}                            onClick={() => setSelectedDays(prev => ({ ...prev, [d]: !prev[d] }))}                          >                            {DAY_LABELS[d].slice(0, 3)}                          </Button>                        ))}                      </div>                    </div>                    <Button onClick={handleCreateRoutine} className="w-full">Crear</Button>                  </div>                </DialogContent>              </Dialog>            </div>          </div>        ) : (          /* Routine exists: show workout UI */          <>            {workoutComplete ? (              <div className="max-w-lg mx-auto text-center space-y-6">                <div className="p-6 rounded-full bg-green-500/20 w-fit mx-auto">                  <Trophy className="h-16 w-16 text-green-500" />                </div>                <h1 className="text-3xl font-bold text-green-500">┬íCompletado!</h1>                <p className="text-muted-foreground">Has terminado tu entrenamiento de hoy</p><p className="text-sm font-semibold text-primary mt-2 flex items-center justify-center gap-1"><Timer className="h-4 w-4" />Duracion: {formatElapsed(elapsedSeconds)}</p>                <Button onClick={() => { setWorkoutComplete(false); setWorkoutStarted(false); setActiveExercise(0); setSessionSets([]); reload(trainingType); }}>                  Volver al Inicio                </Button>              </div>            ) : (              <>                {/* Header */}                <div className="flex items-center justify-between">                  <div>                    <div className="flex items-center gap-2">                      <h1 className="text-2xl md:text-3xl font-extrabold uppercase tracking-tight">                        {todayWorkout?.isWorkoutDay                          ? `Entrenamiento de ${currentExerciseObj?.muscle_group || todayWorkout.dayName}`                          : routine.name}                      </h1>                      <Badge variant={trainingType === 'gimnasio' ? 'default' : 'secondary'}>                        {trainingType === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}                      </Badge>                    </div>                    <p className="text-sm text-muted-foreground">{todayWorkout?.dayName} ┬À {routine.name}</p>                  </div>                  <div className="flex gap-2">                    <Dialog open={addExOpen} onOpenChange={setAddExOpen}>                      <DialogTrigger asChild><Button size="sm" variant="outline"><Plus className="h-4 w-4" /></Button></DialogTrigger>                      <DialogContent>                        <DialogHeader><DialogTitle>Agregar Ejercicio</DialogTitle></DialogHeader>                        <div className="space-y-3">                          <Input placeholder="Nombre del ejercicio" value={newExName} onChange={e => setNewExName(e.target.value)} />                          <Select value={newExDay} onValueChange={setNewExDay}>                            <SelectTrigger><SelectValue placeholder="D├¡a" /></SelectTrigger>                            <SelectContent>                              {DAY_KEYS.filter(d => routine.workout_days[d]).map(d => (                                <SelectItem key={d} value={d}>{DAY_LABELS[d]}</SelectItem>                              ))}                            </SelectContent>                          </Select>                          <Select value={newExMuscle} onValueChange={setNewExMuscle}>                            <SelectTrigger><SelectValue placeholder="Grupo muscular" /></SelectTrigger>                            <SelectContent>                              {MUSCLE_GROUPS.map(g => <SelectItem key={g} value={g}>{g}</SelectItem>)}                            </SelectContent>                          </Select>                          <div className="grid grid-cols-2 gap-2">                            <div>                              <label className="text-xs text-muted-foreground">Series</label>                              <Input type="number" value={newExSets} onChange={e => setNewExSets(Number(e.target.value))} />                            </div>                            <div>                              <label className="text-xs text-muted-foreground">Reps</label>                              <Input value={newExReps} onChange={e => setNewExReps(e.target.value)} />                            </div>                          </div>                          <Button onClick={handleAddExercise} className="w-full">Agregar</Button>                        </div>                      </DialogContent>                    </Dialog>                  </div>                </div>                <Tabs defaultValue="workout">                  <TabsList className="grid grid-cols-4">                    <TabsTrigger value="workout">Entrenamiento</TabsTrigger>                    <TabsTrigger value="progress">Progreso</TabsTrigger>                    <TabsTrigger value="stats">Estad├¡sticas</TabsTrigger>                  <TabsTrigger value="history">Historial</TabsTrigger>                  </TabsList>                  <TabsContent value="stats" className="mt-3">                    <GymStatsView />                  </TabsContent>                  <TabsContent value="progress" className="mt-3">                    <div className="space-y-4">                      <Card className="p-4">                        <h3 className="text-sm font-bold mb-3">­ƒôè Progreso por Ejercicio</h3>                        {exercises.length === 0 ? (                          <p className="text-xs text-muted-foreground text-center py-4">No hay ejercicios en tu rutina</p>                        ) : (                          exercises.map(ex => {                            const exLogs = logs.filter(l => l.exercise_id === ex.id).sort((a, b) => a.log_date.localeCompare(b.log_date));                            if (exLogs.length === 0) return null;                            return (                              <div key={ex.id} className="mb-4 last:mb-0">                                <div className="flex items-center gap-2 mb-2">                                  <span className="text-xs font-semibold">{ex.name}</span>                                  <Badge variant="outline" className="text-[9px]">{ex.target_sets}├ù{ex.target_reps}</Badge>                                </div>                                <div className="overflow-x-auto">                                  <table className="w-full text-[10px]">                                    <thead>                                      <tr className="text-muted-foreground border-b">                                        <th className="text-left py-1 pr-2">Fecha</th>                                        {trainingType === 'gimnasio' && <th className="text-center py-1 px-2">Peso</th>}                                        <th className="text-center py-1 px-2">Reps</th>                                        <th className="text-center py-1 px-2">Series</th>                                        <th className="text-center py-1 px-2 w-12"></th>                                      </tr>                                    </thead>                                    <tbody>                                      {exLogs.slice(-15).map(log => (                                        <tr key={log.id} className="border-b border-muted/30 hover:bg-muted/20 transition-colors group">                                          <td className="py-1 pr-2 text-muted-foreground">{log.log_date.slice(5)}</td>                                          {trainingType === 'gimnasio' && (                                            editingLog === log.id ? (                                              <td className="text-center py-1 px-2">                                                <Input                                                  type="number"                                                  value={editWeight}                                                  onChange={e => setEditWeight(e.target.value)}                                                  className="h-6 w-16 text-[10px] text-center mx-auto"                                                  autoFocus                                                />                                              </td>                                            ) : (                                              <td className="text-center py-1 px-2 font-medium">{log.weight_kg || 'ÔÇö'} kg</td>                                            )                                          )}                                          {editingLog === log.id ? (                                            <>                                              <td className="text-center py-1 px-2">                                                <Input                                                  type="text"                                                  value={editReps}                                                  onChange={e => setEditReps(e.target.value)}                                                  className="h-6 w-20 text-[10px] text-center mx-auto"                                                  placeholder="8,8,7"                                                />                                              </td>                                              <td className="text-center py-1 px-2">{log.sets_completed || log.reps_per_set?.length || 'ÔÇö'}</td>                                              <td className="text-center py-1 px-2">                                                <div className="flex gap-0.5 justify-center">                                                  <Button variant="ghost" size="icon" className="h-5 w-5" onClick={() => {                                                    const w = parseFloat(editWeight);                                                    const r = editReps.split(',').map(Number).filter(n => !isNaN(n));                                                    updateLogEntry(log.id, isNaN(w) ? null : w, r.length > 0 ? r : log.reps_per_set);                                                  }}><Check className="h-3 w-3 text-green-500" /></Button>                                                  <Button variant="ghost" size="icon" className="h-5 w-5" onClick={() => deleteLogEntry(log.id)}><Minus className="h-3 w-3 text-red-500" /></Button>                                                </div>                                              </td>                                            </>                                          ) : (                                            <>                                              <td className="text-center py-1 px-2 cursor-pointer" onClick={() => { setEditingLog(log.id); setEditWeight(String(log.weight_kg || '')); setEditReps((log.reps_per_set || []).join(',')); }}>                                                {log.reps_per_set?.join(', ') || 'ÔÇö'}                                              </td>                                              <td className="text-center py-1 px-2">{log.sets_completed || log.reps_per_set?.length || 'ÔÇö'}</td>                                              <td className="text-center py-1 px-2 opacity-0 group-hover:opacity-100 transition-opacity">                                                <Button variant="ghost" size="icon" className="h-5 w-5" onClick={() => { setEditingLog(log.id); setEditWeight(String(log.weight_kg || '')); setEditReps((log.reps_per_set || []).join(',')); }}>                                                  <ChevronRight className="h-3 w-3" />                                                </Button>                                              </td>                                            </>                                          )}                                        </tr>                                      ))}                                    </tbody>                                  </table>                                </div>                              </div>                            );                          })                        )}                        {logs.length === 0 && exercises.length > 0 && (                          <p className="text-xs text-muted-foreground text-center py-4">A├║n no hay entrenos registrados. ┬íEmpieza a entrenar!</p>                        )}                      </Card>                    </div>                  </TabsContent>                  <TabsContent value="history" className="mt-3">                    <div className="space-y-3">                      {sessionSummaries.length === 0 ? (                        <Card className="p-4 text-center"><p className="text-xs text-muted-foreground">Aun no hay entrenamientos registrados. Completa tu primera sesion!</p></Card>                      ) : (                        <>                          <Card className="p-4"><div className="grid grid-cols-3 gap-2 text-center"><div><p className="text-lg font-bold text-primary">{sessionSummaries.length}</p><p className="text-[10px] text-muted-foreground">sesiones</p></div><div><p className="text-lg font-bold text-primary">{sessionTotals.minutes}</p><p className="text-[10px] text-muted-foreground">min totales</p></div><div><p className="text-lg font-bold text-primary">{Math.round(sessionTotals.volume)}</p><p className="text-[10px] text-muted-foreground">kg volumen</p></div></div></Card>                          {sessionSummaries.map(sm => {                            const start = new Date(sm.started_at);                            const expanded = expandedSession === sm.id;                            return (                              <Card key={sm.id} className="p-4">                                <button className="flex items-center justify-between w-full text-left" onClick={() => setExpandedSession(expanded ? null : sm.id)}>                                  <div>                                    <div className="flex items-center gap-2">                                      <span className="font-semibold capitalize">{start.toLocaleDateString('es-ES', { weekday: 'long', day: 'numeric', month: 'long' })}</span>                                      <Badge variant={sm.tipo === 'gimnasio' ? 'default' : 'secondary'} className="text-[10px]">{sm.tipo === 'gimnasio' ? 'Gimnasio' : 'Calistenia'}</Badge>                                    </div>                                    <p className="text-xs text-muted-foreground mt-0.5">{sm.exerciseCount} ejercicios · {sm.setCount} series · {Math.round(sm.totalVolumeKg)} kg volumen</p>                                  </div>                                  <div className="flex items-center gap-2">                                    <span className="text-xs font-medium text-muted-foreground flex items-center gap-1"><Timer className="h-3 w-3" />{sm.duration_minutes ? `${sm.duration_minutes} min` : '—'}</span>                                    {expanded ? <ChevronUp className="h-4 w-4" /> : <ChevronDown className="h-4 w-4" />}                                  </div>                                </button>                                {expanded && (                                  <div className="mt-3 space-y-2">                                    {sm.exercises.map((ex, i) => (                                      <div key={i} className="flex items-center justify-between px-2 py-1.5 rounded bg-muted/50">                                        <span className="text-sm font-medium">{ex.name}</span>                                        <span className="text-xs text-muted-foreground">{ex.sets} series · {ex.reps.join(', ')} reps · {Math.round(ex.volumeKg)} kg</span>                                      </div>                                    ))}                                  </div>                                )}                              </Card>                            );                          })}                        </>                      )}                    </div>                  </TabsContent>                  <TabsContent value="workout" className="mt-3 space-y-4">                {!todayWorkout?.isWorkoutDay && !workoutStarted ? (                  /* Non-workout day: show routine overview */                  <div className="space-y-4">                    <Card className="p-4 text-center">                      <p className="text-lg font-semibold">Hoy no es d├¡a de entrenamiento</p>                      <p className="text-sm text-muted-foreground mt-1">Revisa tu rutina o agrega ejercicios</p>                      <Button variant="outline" size="sm" className="mt-3" onClick={() => { setTrainingType(trainingType); reload(trainingType); }}>                        Cambiar rutina                      </Button>                    </Card>                    {/* Show exercises by day */}                    <div className="space-y-2">                      {DAY_KEYS.filter(d => routine.workout_days[d]).map(day => {                        const dayExercises = getExercisesByDay(day);                        const isExpanded = viewDay === day;                        return (                          <Card key={day} className="p-3">                            <button                              className="flex items-center justify-between w-full"                              onClick={() => setViewDay(isExpanded ? null : day)}                            >                              <span className="font-semibold">{DAY_LABELS[day]}</span>                              <div className="flex items-center gap-2">                                <Badge variant="secondary">{dayExercises.length} ejercicios</Badge>                                {isExpanded ? <ChevronUp className="h-4 w-4" /> : <ChevronDown className="h-4 w-4" />}                              </div>                            </button>                            {isExpanded && (                              <div className="mt-3 space-y-2">                                {dayExercises.map(ex => (                                  <div key={ex.id} className="flex items-center justify-between px-2 py-1.5 rounded bg-muted/50">                                    <div>                                      <span className="text-sm font-medium">{ex.name}</span>                                      <span className="text-xs text-muted-foreground ml-2">{ex.target_sets}├ù{ex.target_reps}</span>                                      {ex.muscle_group && <Badge variant="outline" className="ml-2 text-[10px]">{ex.muscle_group}</Badge>}                                    </div>                                    <Button size="sm" variant="ghost" className="text-destructive h-7" onClick={() => removeExercise(ex.id)}>├ù</Button>                                  </div>                                ))}                                {dayExercises.length === 0 && <p className="text-xs text-muted-foreground">Sin ejercicios</p>}                              </div>                            )}                          </Card>                        );                      })}                    </div>                    {trainingType === 'gimnasio' && <StrengthGoalsCard />}                  </div>                ) : (                  /* Workout day: active training UI */                  <>                    {!workoutStarted ? (                      <div className="space-y-4">                        <Card className="p-6 text-center">                          {trainingType === 'gimnasio' ? (                            <Dumbbell className="h-12 w-12 mx-auto text-primary mb-3" />                          ) : (                            <Box className="h-12 w-12 mx-auto text-teal-500 mb-3" />                          )}                          <h2 className="text-xl font-bold mb-1">Hoy: {todayWorkout?.dayName}</h2>                          <p className="text-sm text-muted-foreground mb-4">{todayExercises.length} ejercicios programados</p>                          <div className="space-y-2 mb-4">                            {todayExercises.map(ex => (                              <div key={ex.id} className="flex items-center gap-2 px-3 py-2 rounded-lg bg-muted/50">                                {trainingType === 'gimnasio' ? (                                  <Dumbbell className="h-4 w-4 text-muted-foreground" />                                ) : (                                  <Box className="h-4 w-4 text-muted-foreground" />                                )}                                <span className="text-sm font-medium flex-1">{ex.name}</span>                                <span className="text-xs text-muted-foreground">{ex.target_sets}├ù{ex.target_reps}</span>                              </div>                            ))}                          </div>                          <Button size="lg" onClick={() => { sessionStartRef.current = Date.now(); setElapsedSeconds(0); setWorkoutStarted(true); startSession(routine.id, trainingType).then(res => { if (res.data) setActiveSessionId(res.data.id); }); }} className="gap-2 w-full" disabled={todayExercises.length === 0}>                            <Play className="h-5 w-5" /> Iniciar Entrenamiento                          </Button>                        </Card>                      </div>                    ) : (                      /* Active workout */                      <div className="space-y-4">                        <div className="flex items-center justify-center gap-2"><Timer className="h-4 w-4 text-primary" /><span className="text-sm font-bold text-primary tabular-nums">{formatElapsed(elapsedSeconds)}</span><span className="text-xs text-muted-foreground">en curso</span></div>                        {/* Current exercise card */}                        {currentExerciseObj && (                          <Card className="overflow-hidden">                            <div className="bg-gradient-to-r from-primary/10 to-primary/5 p-4">                              <div className="flex items-center justify-between">                                <div>                                  <h2 className="text-lg font-bold">{currentExerciseObj.name}</h2>                                  {currentExerciseObj.muscle_group && (                                    <Badge variant="outline" className="mt-1">{currentExerciseObj.muscle_group}</Badge>                                  )}                                </div>                                <div className="text-right">                                  <span className="text-2xl font-bold text-primary">{completedSets}/{totalSets}</span>                                  <p className="text-xs text-muted-foreground">series</p>                                </div>                              </div>                            </div>                            <CardContent className="p-4 space-y-2">                              {currentExSets.map((set, idx) => {                                const isCompleted = set.completed;                                const isCurrent = idx === currentSetIdx;                                const isPending = !isCompleted && !isCurrent;                                return (                                  <div                                    key={idx}                                    className={cn(                                      "flex items-center gap-3 px-3 py-2.5 rounded-lg border transition-all",                                      isCompleted && "bg-green-500/10 border-green-500/30",                                      isCurrent && "bg-primary/5 border-primary ring-2 ring-primary/30",                                      isPending && "bg-muted/30 border-border opacity-60"                                    )}                                  >                                    {/* Set number */}                                    <div className={cn(                                      "w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold shrink-0",                                      isCompleted && "bg-green-500 text-white",                                      isCurrent && "bg-primary text-primary-foreground",                                      isPending && "bg-muted text-muted-foreground"                                    )}>                                      {isCompleted ? <Check className="h-4 w-4" /> : idx + 1}                                    </div>                                    {/* Weight & reps */}                                    <div className="flex items-center gap-2 flex-1">                                      {trainingType === 'gimnasio' && (                                        <>                                          <Input                                            type="number"                                            value={set.weight}                                            onChange={e => updateSetData(activeExercise, idx, "weight", Number(e.target.value))}                                            className="w-20 h-8 text-center text-sm"                                            disabled={isCompleted}                                            placeholder="kg"                                          />                                          <span className="text-xs text-muted-foreground">kg</span>                                        </>                                      )}                                      <Input                                        type="number"                                        value={set.reps}                                        onChange={e => updateSetData(activeExercise, idx, "reps", Number(e.target.value))}                                        className={cn("h-8 text-center text-sm", trainingType === 'gimnasio' ? "w-16" : "w-20")}                                        disabled={isCompleted}                                      />                                      <span className="text-xs text-muted-foreground">reps</span>                                    </div>                                    {/* Complete button */}                                    {isCurrent && (                                      <Button                                        size="sm"                                        onClick={() => completeSet(activeExercise, idx)}                                        className="h-8 gap-1"                                      >                                        <Check className="h-3 w-3" /> OK                                      </Button>                                    )}                                    {isCompleted && (                                      <Badge className="bg-green-500/20 text-green-600 border-green-500/30 text-xs">Ô£ô</Badge>                                    )}                                  </div>                                );                              })}                            </CardContent>                          </Card>                        )}                        {/* Rest timer */}                        {restTimer !== null && restTimer > 0 && (                          <Card className="p-6 text-center bg-gradient-to-b from-primary/5 to-background">                            <p className="text-xs uppercase tracking-widest text-muted-foreground mb-2">Temporizador de Descanso</p>                            <p className="text-6xl font-bold text-primary tabular-nums">                              {String(Math.floor(restTimer / 60)).padStart(2, "0")}:{String(restTimer % 60).padStart(2, "0")}                            </p>                            <p className="text-xs text-muted-foreground mt-3">                              Siguiente: {currentExerciseObj?.name} ┬À Serie {(currentSetIdx !== -1 ? currentSetIdx : totalSets) + 1}/{totalSets}                            </p>                            <div className="flex items-center justify-center gap-3 mt-4">                              <Button size="sm" variant="outline" className="rounded-full w-10 h-10 p-0" onClick={() => adjustRest(-5)}>                                <Minus className="h-4 w-4" />                              </Button>                              <span className="text-xs text-muted-foreground">-5s / +5s</span>                              <Button size="sm" variant="outline" className="rounded-full w-10 h-10 p-0" onClick={() => adjustRest(5)}>                                <Plus className="h-4 w-4" />                              </Button>                              <Button size="sm" variant="destructive" onClick={skipRest} className="ml-2 gap-1">                                <SkipForward className="h-4 w-4" /> Omitir                              </Button>                            </div>                          </Card>                        )}                        {/* Next exercise preview */}                        {activeExercise < todayExercises.length - 1 && (                          <Card className="p-3 opacity-60">                            <div className="flex items-center gap-2">                              {trainingType === 'gimnasio' ? (                                <Dumbbell className="h-4 w-4 text-muted-foreground" />                              ) : (                                <Box className="h-4 w-4 text-muted-foreground" />                              )}                              <span className="text-sm font-medium flex-1">{todayExercises[activeExercise + 1].name}</span>                              <span className="text-xs text-muted-foreground">                                0/{todayExercises[activeExercise + 1].target_sets}                              </span>                            </div>                          </Card>                        )}                        {/* Next button */}                        <Button                          size="lg"                          className="w-full gap-2"                          onClick={handleNext}                          disabled={!allSetsComplete && restTimer === null}                        >                          {restTimer !== null && restTimer > 0 ? (                            <><SkipForward className="h-5 w-5" /> Omitir Descanso</>                          ) : allSetsComplete ? (                            <><ChevronRight className="h-5 w-5" /> {activeExercise < todayExercises.length - 1 ? "Siguiente Ejercicio" : "Finalizar"}</>                          ) : (                            <><Timer className="h-5 w-5" /> Completa la serie actual</>                          )}                        </Button>                      </div>                    )}                  </>                )}                  </TabsContent>                </Tabs>              </>            )}          </>        )}      </div>    </div>  );}